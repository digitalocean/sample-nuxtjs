{
  "name": "sample-nuxtjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "rm -r _static 2>/dev/null; nuxi generate && mv .output/public _static && (rm _static/README.md || true)",
    "start": "node sammy.js; serve -s _static -l tcp://0.0.0.0:${PORT:-8080} -n",
    "export": "nuxt export",
    "serve": "nuxt serve"
  },
  "dependencies": {
    "nuxt": "^4.0.3",
    "serve": "^14.2.3"
  },
  "devDependencies": {}
}
